<!DOCTYPE html>
<html>
  <meta http-equiv="refresh" content="1; url=?id={{id}}" />

  <body>
    <img src="logo.png" />
    <h1 id="room">{{room}}</h1>
    <table id="users">
      {{#users}}
        <tr>
          <td class="name">{{name}}</td>
        </tr>
      {{/users}}
    </table>
  </body>
</html> 
